basePath = '../'; // all relative paths in this file will be based on this (and this is based on the location of this file)

files = [
  JASMINE,
  JASMINE_ADAPTER,
  'static/*.js',
  'tests/*.js'
];

autoWatch = true; // re-run tests when they change on disk
